# AWS Configuration
AWS_PROFILE=default
AWS_REGION=us-east-1

# Cleanup Configuration
# Set to "true" to skip the confirmation prompt
SKIP_CONFIRMATION=false
# Set to "true" to perform a dry run (list resources without deleting)
DRY_RUN=false
